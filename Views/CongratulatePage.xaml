<Page x:Class="MoyuWindows.Views.CongratulatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="350"
      Title="完成"
      Background="Transparent"
      Loaded="Page_Loaded">
    
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <!-- 庆祝图标 -->
            <TextBlock Text="🎉"
                       FontSize="72"
                       HorizontalAlignment="Center"/>
            
            <!-- 标题 -->
            <TextBlock Text="太棒了！"
                       Style="{StaticResource HeadingLarge}"
                       HorizontalAlignment="Center"
                       Margin="0,16,0,0"/>
            
            <TextBlock Text="今日学习已完成"
                       Style="{StaticResource BodyText}"
                       HorizontalAlignment="Center"
                       Margin="0,8,0,0"/>
            
            <!-- 统计卡片 -->
            <Border Style="{StaticResource Card}"
                    Margin="0,32,0,0"
                    MinWidth="280">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- 学习数量 -->
                    <StackPanel Grid.Column="0" Grid.Row="0" 
                                HorizontalAlignment="Center"
                                Margin="0,0,0,16">
                        <TextBlock x:Name="LearnedCountText"
                                   Text="20"
                                   FontSize="32"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="学习单词"
                                   Style="{StaticResource MutedText}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 正确率 -->
                    <StackPanel Grid.Column="1" Grid.Row="0"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,16">
                        <TextBlock x:Name="AccuracyText"
                                   Text="85%"
                                   FontSize="32"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource SecondaryBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="正确率"
                                   Style="{StaticResource MutedText}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 正确数 -->
                    <StackPanel Grid.Column="0" Grid.Row="1"
                                HorizontalAlignment="Center">
                        <TextBlock x:Name="CorrectCountText"
                                   Text="17"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource SuccessBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="正确"
                                   Style="{StaticResource MutedText}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                    
                    <!-- 错误数 -->
                    <StackPanel Grid.Column="1" Grid.Row="1"
                                HorizontalAlignment="Center">
                        <TextBlock x:Name="WrongCountText"
                                   Text="3"
                                   FontSize="24"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource DangerBrush}"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="错误"
                                   Style="{StaticResource MutedText}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 鼓励语 -->
            <TextBlock x:Name="MotivationText"
                       Text="坚持就是胜利，明天继续加油！"
                       Style="{StaticResource BodyText}"
                       HorizontalAlignment="Center"
                       Margin="0,24,0,0"/>
            
            <!-- 按钮 -->
            <StackPanel Orientation="Horizontal" 
                        HorizontalAlignment="Center"
                        Margin="0,32,0,0">
                <Button Content="查看错词"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,16,0"
                        Click="ViewWrongBook_Click"/>
                <Button Content="返回首页"
                        Style="{StaticResource PrimaryButton}"
                        Click="BackToHome_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
